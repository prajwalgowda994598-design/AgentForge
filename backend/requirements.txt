# ─────────────────────────────────────────────────────────────────────────────
# AgentForge – Python Backend Dependencies
#
# Pinned versions for reproducible builds.
# These match the tested local environment (Python 3.12+).
# ─────────────────────────────────────────────────────────────────────────────

# ── Web Framework ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
gunicorn==22.0.0
python-multipart==0.0.9

# ── Async / HTTP ───────────────────────────────────────────────────────────────
httpx==0.27.0
aiofiles==23.2.1
websockets==12.0

# ── Async SQLite (local dev + tests, no Docker needed) ────────────────────────
aiosqlite==0.19.0

# ── LangChain Ecosystem ────────────────────────────────────────────────────────
langchain==0.2.6
langchain-core==0.2.22
langchain-community==0.2.6
langchain-openai==0.1.14
langgraph==0.1.14

# ── OpenAI ─────────────────────────────────────────────────────────────────────
openai==1.35.7

# ── Vector Store ──────────────────────────────────────────────────────────────
faiss-cpu==1.8.0
numpy==1.26.4

# ── Embeddings (local, free — downloads ~90 MB on first run) ─────────────────
sentence-transformers==3.0.1
langchain-huggingface==0.0.3

# ── Database – PostgreSQL (production) ────────────────────────────────────────
sqlalchemy==2.0.31
asyncpg==0.29.0
alembic==1.13.2
psycopg2-binary==2.9.9

# ── Database – Neo4j ──────────────────────────────────────────────────────────
neo4j==5.22.0

# ── Database – Redis / FakeRedis ──────────────────────────────────────────────
redis==5.0.7
fakeredis==2.23.3         # in-memory Redis for local dev and tests

# ── Configuration & Validation ────────────────────────────────────────────────
pydantic[email]==2.8.2
pydantic-settings==2.3.4
email-validator==2.2.0
python-dotenv==1.0.1

# ── Auth ──────────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3

# ── Logging ───────────────────────────────────────────────────────────────────
structlog==24.2.0

# ── Resilience ────────────────────────────────────────────────────────────────
tenacity==8.5.0

# ── Web Search Tool ───────────────────────────────────────────────────────────
duckduckgo-search==6.2.6

# ── Document Processing ───────────────────────────────────────────────────────
beautifulsoup4==4.12.3
pymupdf==1.24.7

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-mock==3.14.0
pytest-cov==5.0.0
httpx==0.27.0             # async test client for FastAPI

# ── Linting / Formatting (dev only) ───────────────────────────────────────────
ruff==0.5.1
mypy==1.10.1
